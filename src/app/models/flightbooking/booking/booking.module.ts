export interface BookingModule{
  flightBookingId: number;
  departureCity: string;
  arrivalCity: string;
  departureDate: Date;
  arrivalDate: Date;
  noOfPassenger: number;
  flightId: number;
  userId: number;
}
